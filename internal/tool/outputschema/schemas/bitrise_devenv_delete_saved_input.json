{
  "description": "Response of DELETE /v1/saved-inputs/{savedInputId} (Bitrise Remote Dev Environments API, HTTP 200). The API returns an empty body on success, so content is empty.",
  "properties": {
    "content": {
      "description": "The text result of the tool.",
      "type": "string"
    }
  },
  "type": "object"
}
