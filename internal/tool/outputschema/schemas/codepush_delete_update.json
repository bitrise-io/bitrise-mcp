{
  "description": "Response of DELETE /updates/{id} (Release Management API - CodePush, HTTP 204). The API returns an empty body on success, so content is empty.",
  "properties": {
    "content": {
      "description": "The text result of the tool.",
      "type": "string"
    }
  },
  "type": "object"
}
