{
  "description": "Response of POST /apps/{app-slug}/pipelines/{pipeline-id}/rebuild (Bitrise API, HTTP 201). The API returns an empty body on success, so content is empty.",
  "properties": {
    "content": {
      "description": "The text result of the tool.",
      "type": "string"
    }
  },
  "type": "object"
}
