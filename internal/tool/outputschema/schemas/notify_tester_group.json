{
  "description": "Response of POST /tester-groups/{id}/notify (Release Management API - Build Distributions, HTTP 204). The API returns an empty body on success, so content is empty.",
  "properties": {
    "content": {
      "description": "The text result of the tool.",
      "type": "string"
    }
  },
  "type": "object"
}
