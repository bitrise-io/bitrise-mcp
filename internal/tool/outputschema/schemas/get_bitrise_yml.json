{
  "description": "Response of GET /apps/{app-slug}/bitrise.yml (Bitrise API, HTTP 200). The raw text body of the response.",
  "properties": {
    "content": {
      "description": "The text result of the tool.",
      "type": "string"
    }
  },
  "type": "object"
}
