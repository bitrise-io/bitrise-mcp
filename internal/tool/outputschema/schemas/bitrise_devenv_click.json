{
  "description": "Response of POST /v1/workspaces/{workspaceId}/sessions/{sessionId}/click (Bitrise Remote Dev Environments API, HTTP 200). The API returns an empty body on success, so content is empty.",
  "properties": {
    "content": {
      "description": "The text result of the tool.",
      "type": "string"
    }
  },
  "type": "object"
}
