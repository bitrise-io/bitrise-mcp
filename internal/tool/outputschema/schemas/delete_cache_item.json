{
  "description": "Response of DELETE /apps/{app-slug}/cache-items/{cache-item-id} (Bitrise API, HTTP 200). The API returns an empty body on success, so content is empty.",
  "properties": {
    "content": {
      "description": "The text result of the tool.",
      "type": "string"
    }
  },
  "type": "object"
}
